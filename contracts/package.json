{
  "name": "@cloudgreen/green-credit-contracts",
  "version": "1.0.0",
  "description": "CloudGreen OS GreenCredit ERC-20 token — carbon credit smart contracts",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:amoy": "npx hardhat run scripts/deploy.js --network polygonAmoy",
    "verify:amoy": "npx hardhat run scripts/verify.js --network polygonAmoy",
    "clean": "npx hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17"
  }
}
